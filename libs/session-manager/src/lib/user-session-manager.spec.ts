import { userSessionManager } from './user-session-manager';

describe('userSessionManager', () => {
  it('should work', () => {
    expect(userSessionManager()).toEqual('user-session-manager');
  });
});
