# express-app

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build express-app` to build the library.

## Running unit tests

Run `nx test express-app` to execute the unit tests via [Jest](https://jestjs.io).
